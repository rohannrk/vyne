# Database — Neon hosted Postgres (neon.tech → new project → connection string)
DATABASE_URL=""

# Auth.js — generate: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL=""

# GitHub OAuth App — create at: github.com → Settings → Developer Settings → OAuth Apps
# Homepage URL: http://localhost:3000
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
